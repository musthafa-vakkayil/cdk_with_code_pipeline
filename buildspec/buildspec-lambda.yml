version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: latest
    commands:
      - cd src
      - npm ci
  build:
    commands:
      - npm run build

artifacts:
  files:
    - 'src/dist/**'
  discard-paths: no
